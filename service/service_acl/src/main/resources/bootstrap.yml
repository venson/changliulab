spring:
  application:
    name: service-acl
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        shared-configs:
          - data-id: common-mysql.yaml

