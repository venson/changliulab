spring:
  application:
    name: service-cms
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        shared-configs:
          - data-id: common-mysql.yaml
          - data-id: common-redis.yaml
