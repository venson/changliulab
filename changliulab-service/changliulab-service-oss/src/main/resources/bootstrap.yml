spring:
  cloud:
    nacos:
      config:
        name: service-oss-dev
        server-addr: localhost:8848
        file-extension: yaml
  profiles:
    active: dev
  application:
    name: service-oss
